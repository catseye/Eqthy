#!/usr/bin/env python3

from os.path import realpath, dirname, join
import sys

sys.path.insert(0, join(dirname(realpath(sys.argv[0])), '..', 'src'))

from eqthy.main import main


if __name__ == '__main__':
    main(sys.argv[1:])
